---
import LayoutMaster from '../layouts/LayoutMaster.astro';

// Quando trasformeremo tutto in JSON, questo dominio lo prenderemo dal data.json
// Per ora mettiamo il tuo link provvisorio per farlo funzionare subito.
const dominioSito = "https://webora-01a.pages.dev";
---

<LayoutMaster>
  
  <header class="relative pt-40 pb-20 flex flex-col items-center justify-center text-center border-b border-white/5 bg-black">
    <div class="relative z-10 px-4 reveal">
      <h1 class="text-4xl md:text-5xl font-bold text-[var(--colorNocciola)] mb-4 tracking-widest font-elegante uppercase">
        Centro Stampa QR Code
      </h1>
      <div class="w-16 h-px bg-[var(--colorRosso)] mx-auto mb-6"></div>
      <p class="text-gray-300 text-xs md:text-sm font-light tracking-widest uppercase max-w-xl leading-relaxed">
        Strumento riservato alla gestione del locale. Salva o stampa i codici QR per le tue operazioni di marketing e per il servizio ai tavoli.
      </p>
    </div>
  </header>

  <section class="py-16 bg-[#0a0705] min-h-screen">
    <div class="max-w-5xl mx-auto px-4 lg:px-8">

      <div class="text-center mb-12 reveal">
        <button onclick="window.print()" class="inline-block bg-[var(--colorNocciola)] text-black px-8 py-3 font-bold tracking-widest uppercase text-xs hover:bg-white transition-colors shadow-lg cursor-pointer">
          Stampa questa pagina
        </button>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 gap-12 reveal">
        
        <div class="bg-[#120d0b] border border-white/5 p-8 flex flex-col items-center text-center rounded-sm shadow-2xl relative">
          <div class="absolute top-0 left-0 w-full h-1 bg-gradient-to-r from-transparent via-[var(--colorNocciola)] to-transparent opacity-50"></div>
          
          <h2 class="text-2xl font-bold text-white tracking-widest uppercase font-elegante mb-2">QR Code Generale</h2>
          <p class="text-gray-400 text-xs tracking-widest uppercase mb-8">Uso: Vetrina, Biglietti da Visita, Social</p>
          
          <div class="bg-white p-4 rounded-sm shadow-[0_0_30px_rgba(255,255,255,0.1)] mb-6">
            <img src={`https://api.qrserver.com/v1/create-qr-code/?size=400x400&data=${dominioSito}/`} alt="QR Code Vetrina" class="w-48 h-48" />
          </div>

          <p class="text-gray-500 text-xs font-light leading-relaxed">
            Questo codice porta alla <strong class="text-white">Home Page</strong>. Serve per presentare il locale, far vedere l'eleganza degli ambienti e smistare i clienti verso le prenotazioni o le recensioni.
          </p>
        </div>

        <div class="bg-[#120d0b] border border-white/5 p-8 flex flex-col items-center text-center rounded-sm shadow-2xl relative">
          <div class="absolute top-0 left-0 w-full h-1 bg-gradient-to-r from-transparent via-[var(--colorRosso)] to-transparent opacity-50"></div>
          
          <h2 class="text-2xl font-bold text-[var(--colorNocciola)] tracking-widest uppercase font-elegante mb-2">QR Code Tavolo</h2>
          <p class="text-[var(--colorNocciola)] text-xs tracking-widest uppercase mb-8">Uso: Segnaposto sui Tavoli fisici</p>
          
          <div class="bg-white p-4 rounded-sm shadow-[0_0_30px_rgba(212,175,55,0.2)] mb-6 border-2 border-[var(--colorNocciola)]">
            <img src={`https://api.qrserver.com/v1/create-qr-code/?size=400x400&data=${dominioSito}/menu`} alt="QR Code Tavolo" class="w-48 h-48" />
          </div>

          <p class="text-gray-500 text-xs font-light leading-relaxed">
            Questo codice porta al <strong class="text-white">Menù Veloce</strong>. Salta la presentazione e permette al cliente seduto di consultare immediatamente i piatti, ordinare o accedere al pagamento.
          </p>
        </div>

      </div>

    </div>
  </section>

</LayoutMaster>

<style>
  @media print {
    body { background-color: white !important; color: black !important; }
    nav, footer, button { display: none !important; }
    .bg-\[\#120d0b\], .bg-\[\#0a0705\], .bg-black { background-color: white !important; border: 1px solid #ccc !important; }
    .text-white, .text-gray-300, .text-gray-400, .text-gray-500 { color: black !important; }
    .shadow-2xl { box-shadow: none !important; }
  }
</style>